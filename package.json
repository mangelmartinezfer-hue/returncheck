{
  "name": "returncheck",
  "version": "1.0.0",
  "description": "ReturnCheck — can this specific product actually be returned? Paid check_return API for shopping agents. US first, global-ready.",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "node tools/build-info.mjs",
    "build-info": "node tools/build-info.mjs",
    "pretest": "node tools/build-info.mjs",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "predeploy": "node tools/build-info.mjs",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
